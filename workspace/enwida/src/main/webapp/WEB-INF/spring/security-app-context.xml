<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
                    http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                    http://www.springframework.org/schema/security 
                    http://www.springframework.org/schema/security/spring-security-3.1.xsd">
  
<authentication-manager>
	   <authentication-provider>
		<jdbc-user-service data-source-ref="dataSource"
 
		   users-by-username-query="
		      select user_name,user_password, enabled 
		      from users.users where user_name=?" 
 
		   authorities-by-username-query="     
				SELECT users.user_name, roles.role_name FROM users.users
	    		 INNER JOIN users.user_group ON user_group.user_id=users.user_id 
	    		 INNER JOIN users.group_role ON group_role.group_id=user_group.group_id
	    		 INNER JOIN users.roles ON roles.role_ID=group_role.role_id
	    		 WHERE user_name=?" 
 
		/>
	   </authentication-provider>
	</authentication-manager>
	
	<beans:bean id="rememberMeAuthenticationProvider" class="org.springframework.security.authentication.RememberMeAuthenticationProvider">
	    <beans:constructor-arg name="key" value="token"/>
	</beans:bean>
	
 	 <beans:bean id="loginSuccessHandler" class="de.enwida.web.controller.LoginSuccessHandler">
		 <beans:property name="defaultTargetUrl" value="/user/" />
		 <beans:property name="useReferer" value="true"/>
	 </beans:bean>

	<beans:bean id="authenticationEntryPoint" class="de.enwida.web.controller.TestLoginUrlAuthenticationEntryPoint">
 		<beans:property name="loginFormUrl" value="/user/login.html"/>
	</beans:bean>

    <http use-expressions="true" auto-config="true" entry-point-ref="authenticationEntryPoint">
<!--  		<intercept-url pattern="/dashboard" access="hasRole('ROLE USER')"/> -->
	    <intercept-url pattern="/" access="permitAll" />
	    <intercept-url pattern="/*" access="permitAll" />
	    <intercept-url pattern="/j_spring_security_check" access="hasRole('ROLE USER')"/>
 	    <intercept-url pattern="/user/admin/*" access="hasRole('admin')" />
	    
	    <form-login  login-page="/user/login.html"  authentication-failure-url="/user/loginFailed" login-processing-url="/j_spring_security_check" 
	     authentication-success-handler-ref="loginSuccessHandler" default-target-url="/user/login.html"  always-use-default-target="false"/>        
	    <logout logout-success-url="/" logout-url="/j_spring_security_logout" />
	    <access-denied-handler error-page="/403.html"/>
	     <remember-me key="!casAA='0401./e;2009NrI" use-secure-cookie="true" />
    </http>
</beans:beans>   